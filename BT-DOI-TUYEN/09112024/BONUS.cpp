#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define endl "\n"
int main(){
    ll n;
    cin>>n;
    vector<ll> a(n);
    for(int i =0 ; i , n; i++)cin>>a[i];
    sort(a.begin(), a.end());
    vector<ll> luu2(n, 0);
    for(ll i = 0; i < n; i++){
        
    }
}