#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define endl "\n"
#define int long long
#define faster ios_base::syn_with_stdio(0) cin.tie(0) cout.tie(0);
#define King signed main()
bool able(vector<int>& a, ll check)
{
    if(check > a.size())return false;
    sort(a.begin(), a.end());
}
King
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll n, x, res = 0;
        cin>>n>>x;
        vector<ll> a(n);
        for(int i = 0; i < n; i++)cin>>a[i];
    }
    
}